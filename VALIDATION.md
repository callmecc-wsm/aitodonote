# 构建与验证状态

## v0.2.0 验收

第二轮验收通过，正在增加最低支持版本 API 26 和弹层几何断言。第二轮源码：`d09f9214ca2272aaef6af2b2a958e32bb2ce267a`。

[构建与设备测试](https://github.com/callmecc-wsm/aitodonote/actions/runs/34854929380)

| 检查 | 范围 |
| --- | --- |
| 构建与静态检查 | APK 编译、Android Lint、APK v2 签名验证 |
| JavaScript | 8 项纯逻辑测试：分类、文本转义、推进状态和优先级 |
| Android JVM | 10 项规则测试：回顾、提醒、安静时段、端点与来源链接 |
| Android 10 / API 29 | 47 项设备测试通过 |
| Android 15 / API 35 | 同一组 47 项设备测试通过 |

设备测试包括：

- SQLite 原文、事件、备份合并、导入事务回滚、更新顺序与草稿存储。
- 连续研究间隔和 3 轮上限；等用户回复；筛除现实行动和仅记录；失败停止；编辑或取消时丢弃旧结果；每轮最多 3 次尝试。
- 真实 Android 通知、独立快捷操作、重复完成、旧通知失效、关闭或安静时段后的补发、未读并发保护。
- 模型请求结构、单条搜索开关、历史上下文、来源过滤、错误格式处理、接口和密钥快照。
- 真实本地 HTTPS：中文 JSON、Authorization、拒绝重定向、401/403/404/429/500、超大响应限制。
- WorkManager 实际 Worker：无模型也能提醒、关闭自动回顾、错误暂停、按记录排队、启动失败重试上限。
- 实际 WebView：首页、设置、原生记录写入、草稿在 Activity 重建后恢复、未读状态和不执行 HTML。

## 验证方式与边界

模型语义测试采用注入的固定响应；HTTPS 测试连接模拟器本地 TLS 服务。它们验证协议、数据流和应用行为，**不代表真实模型推理或 Tavily 检索效果已验收**。真实服务需要用户自己的有效 API Key。

尚未验证真实手机厂商的后台省电策略、长时间无人值守运行、手机重启后的实际调度时刻；后台回顾不保证准点。已测试 Activity 重建与持久化，不将其描述为完整的断电恢复测试。

CI 保存模拟器截图和运行日志；当前环境无法查看下载出的截图，界面验收依据是实际 WebView 断言，没有声称完成截图视觉验收。

安装包来自通过验收的同一源码提交。CI 开发签名是临时生成的，后续构建可能需要先导出、卸载再安装；文档更新不会重新生成 APK。

## 首轮发现与修正

[首轮设备验收](https://github.com/callmecc-wsm/aitodonote/actions/runs/34854041569) 的 45 项设备测试中，API 29 有 1 项失败，API 35 有 2 项失败。修正通知异步可见性的等待与 WebView 返回字符串的断言后复测；额外补上旧页面不能标记新进展已读、启动失败有界重试的测试。

## 历史 v0.1.0

[2026-09-07 构建](https://github.com/callmecc-wsm/aitodonote/actions/runs/34119925870)：5 项 UI 逻辑、8 项 JVM、7 项 API 29 设备测试通过，源码 `c2b92d1c20be05cbddd006a107ab803685523dc9`。

交付使用 Actions 附件。先前 Release 发布受到 GitHub 集成权限限制，未创建 Release。
